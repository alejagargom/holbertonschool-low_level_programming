#include "holberton.h"

/**
 * _isupper- checks JA.
 *@JA: checking if JA is  JA UPPERCASE
 *
 * Return:A  UPPERCASE Z
 */
int _isupper(int JA)
{
	if (JA >= 'A' &&  JA <= 'Z')
	{
		return (1);
	}
	else
	{
	return (0);
	}
}
