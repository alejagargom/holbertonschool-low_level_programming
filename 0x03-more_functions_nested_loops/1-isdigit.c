#include "holberton.h"

/**
 * _isdigit- retunr d.
 *@d: checking if d is digit
 *
 * Return: d in digit 1 and 0;
 */
int _isdigit(int d)
{
	if (d >= 48  &&  d <= 57)
		return (1);

	else
	return (0);

}
